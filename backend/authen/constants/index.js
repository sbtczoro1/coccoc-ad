const AuthValidateMessage = {
  Enter_VALID_EMAIL: "Enter a valid email address",
  PASSWORD_AT_LEAST: "Password must be at least x chars long",
};

const AuthErrorMessage = {
  USER_INFO_WRONG: "User info is not correct",
  USER_NOT_EXIST: "is not exists",
};

const LoginRules = {
  PASSWORD_MIN_LENGTH: 5,
};

module.exports = {
  AuthValidateMessage,
  AuthErrorMessage,
  LoginRules,
};
